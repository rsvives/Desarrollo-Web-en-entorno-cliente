class circle {
	constructor(circle) {
		this.circle = circle;
	}
}
function calcCircle() {
	let height = alert('Dame la altura del circulo');
	let diameter = alert('Dame el diametro del circulo');
	alert('El volumen del circulo es:' + 3.14 * diameter * diameter * height);
}
